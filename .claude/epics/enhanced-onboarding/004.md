---
name: Implement Step 1 (User Profile) and Step 2 (Product Intro)
status: open
created: 2026-02-02T01:33:47Z
updated: 2026-02-02T01:33:47Z
github: "[Will be updated when synced to GitHub]"
depends_on: [003]
parallel: true
conflicts_with: []
---

# Task: Implement Step 1 (User Profile) and Step 2 (Product Intro)

## Description
Replace the Step 1 placeholder with the existing profile/preferences form (migrated from current `OnboardingPage`). Replace the Step 2 placeholder with a static product introduction screen explaining Traxelos One.

## Acceptance Criteria
- [ ] Step 1 shows: name field (optional), use case selection (required), referral source (optional)
- [ ] Step 1 "Continue" validates use case is selected
- [ ] Step 1 "Skip for now" sets use case to "skipped" and advances to Step 2
- [ ] Step 2 shows: product explanation (what Traxelos One is, what you need, what's next)
- [ ] Step 2 has icon/illustration showing device + phone concept
- [ ] Step 2 has "Continue" button only (no skip needed — informational)
- [ ] Both steps use `AppColors` and `Theme.of(context).textTheme` — no hardcoded styles
- [ ] Proper `Semantics` labels on all interactive elements

## Technical Details
- Step 1: Migrate form fields from existing `OnboardingPage` code into a step widget
  - Use case options: Inventory, Habit, Fitness, Manufacturing, Other
  - Referral options: Friend, Social Media, Search, App Store, Other
  - Form data stored in parent `OnboardingPage` state (passed via callbacks)
- Step 2: Static content widget
  - Use `Icons.bluetooth_connected` or similar for illustration placeholder
  - Three info points: what it is, what you need, what's next
  - Clean layout with centered content
- Both steps are private widgets within the onboarding feature

## Dependencies
- [ ] Task 003 — needs PageView skeleton to plug into

## Effort Estimate
- Size: S
- Parallel: true (can run alongside Tasks 005, 006)
