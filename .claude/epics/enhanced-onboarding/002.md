---
name: Add onboarding tracking fields to User entity/model/repository
status: done
created: 2026-02-02T01:33:47Z
updated: 2026-02-02T01:33:47Z
github: "[Will be updated when synced to GitHub]"
depends_on: []
parallel: true
conflicts_with: []
---

# Task: Add onboarding tracking fields to User entity/model/repository

## Description
Add `onboardingDevicePaired` (bool, default false) and `onboardingItemCreated` (bool, default false) to the User entity, UserModel (Firestore serialization), and extend `completeOnboarding()` to accept and write these fields using `SetOptions(merge: true)`.

## Acceptance Criteria
- [ ] `User` entity has `onboardingDevicePaired` and `onboardingItemCreated` fields (default: false)
- [ ] `UserModel` serializes/deserializes the new fields to/from Firestore (`onboarding_device_paired`, `onboarding_item_created`)
- [ ] `completeOnboarding()` accepts optional `devicePaired` and `itemCreated` parameters
- [ ] Existing `completeOnboarding()` callers are unaffected (new params have defaults)
- [ ] Unit tests for serialization round-trip of new fields
- [ ] Unit test for `completeOnboarding()` writing new fields

## Technical Details
- Files affected:
  - `lib/features/auth/domain/entities/user.dart` — add fields to entity
  - `lib/features/auth/data/models/user_model.dart` — add Firestore serialization
  - `lib/features/auth/data/repositories/user_repository_impl.dart` — extend `completeOnboarding()`
- Use `SetOptions(merge: true)` so existing fields are preserved
- New fields default to `false` for backward compatibility with existing users

## Dependencies
- [ ] None — data model change only

## Effort Estimate
- Size: XS
- Parallel: true
