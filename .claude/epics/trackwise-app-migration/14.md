---
name: FlutterFlow Cleanup
status: open
created: 2026-01-15T04:39:47Z
updated: 2026-01-15T04:50:13Z
github: https://github.com/roger6272/trackwise/issues/14
depends_on: [9, 10, 11, 12, 13]
parallel: false
conflicts_with: []
---

# Task: FlutterFlow Cleanup

## Description

Remove all FlutterFlow dependencies from the codebase. Delete FF directories, remove FF imports from feature code, and remove the migration flags system.

## Acceptance Criteria

- [ ] `lib/flutter_flow/` directory deleted
- [ ] `lib/account_profile_creation/` directory deleted
- [ ] `lib/components/` directory deleted
- [ ] No `flutter_flow` imports in `lib/features/`
- [ ] No `flutter_flow` imports in `lib/core/`
- [ ] `MigrationFlags` class removed
- [ ] All migration flag checks removed from code
- [ ] `app_state.dart` removed (if FF-specific)
- [ ] App builds successfully (`flutter build apk --debug`)
- [ ] All tests pass (`flutter test`)
- [ ] App runs without errors

## Technical Details

### Directories to Delete
- `lib/flutter_flow/` (18 files)
- `lib/account_profile_creation/` (24 files)
- `lib/components/` (2 files)

### Files to Modify
- `lib/main.dart` - Remove migration flag conditionals
- `lib/core/config/migration_flags.dart` - DELETE entire file
- Any files importing from FF directories

### Verification Steps
```bash
# Check for remaining FF imports
grep -r "flutter_flow" lib/features/
grep -r "flutter_flow" lib/core/
grep -r "account_profile_creation" lib/
grep -r "MigrationFlags" lib/

# All should return empty
```

### Implementation Steps
1. Search for all FF imports in features/core
2. Replace any remaining FF utilities with new equivalents
3. Remove migration flag conditionals from main.dart
4. Delete `migration_flags.dart`
5. Delete `flutter_flow/` directory
6. Delete `account_profile_creation/` directory
7. Delete `components/` directory
8. Run `flutter build apk --debug`
9. Run `flutter test`
10. Manual smoke test of app

### Key Considerations
- Keep `lib/auth/` (Firebase auth utils, not FF)
- Keep `lib/backend/` (Firebase config, not FF)
- Some utilities may still be needed - ensure replacements exist
- This is irreversible - ensure all flows work first

## Dependencies

- [x] Task 9 (Auth Flow) complete and working
- [x] Task 10 (Items Flow) complete and working
- [x] Task 11 (Bluetooth Pages) complete and working
- [x] Task 12 (Profile & Export) complete and working
- [x] Task 13 (Widget Tests) complete and passing

## Effort Estimate

- Size: S
- Hours: 2-3
- Parallel: false (must be last task)

## Definition of Done

- [ ] All FF directories deleted
- [ ] No FF imports remain
- [ ] Migration flags removed
- [ ] Build succeeds
- [ ] Tests pass
- [ ] Manual smoke test passes
