---
name: Auth Flow Integration
status: completed
created: 2026-01-15T04:39:47Z
updated: 2026-01-15T06:15:00Z
github: https://github.com/roger6272/trackwise/issues/9
depends_on: [8]
parallel: true
conflicts_with: []
---

# Task: Auth Flow Integration

## Description

Wire up the existing auth pages (login, signup, forgot password) to the AppRouter and ensure all authentication providers (email, Google, Apple) work correctly.

## Acceptance Criteria

- [x] Login page accessible at `/login` route
- [x] Signup page accessible at `/signup` route
- [x] Forgot password page accessible at `/forgot-password` route
- [x] Email/password sign-in works
- [x] Email/password sign-up works
- [x] Google Sign-In works
- [x] Apple Sign-In works
- [x] Password reset email sends successfully
- [x] Auth state changes navigate to correct pages (login → home, logout → login)
- [x] Session persistence works (app restart keeps user logged in)

## Technical Details

### Files to Modify
- `lib/core/router/app_router.dart` - Add/verify auth routes
- `lib/features/auth/presentation/pages/login_page.dart` - Verify BLoC integration
- `lib/features/auth/presentation/pages/signup_page.dart` - Verify BLoC integration
- `lib/features/auth/presentation/pages/forgot_password_page.dart` - Verify BLoC integration

### Existing Assets
- `AuthBloc` at `lib/features/auth/presentation/bloc/`
- Auth use cases (sign_in_with_email, sign_in_with_google, sign_in_with_apple, etc.)
- Firebase auth already configured

### Implementation Steps
1. Add auth routes to `AppRouter` if not present
2. Ensure `AuthBloc` is provided at app level
3. Connect login page to `AuthBloc` events
4. Connect signup page to `AuthBloc` events
5. Connect forgot password page to `AuthBloc` events
6. Test each auth provider
7. Test auth state navigation (redirect guards)

### Key Considerations
- Use existing `auth/firebase_auth/` utilities for provider integration
- Ensure error handling displays user-friendly messages
- Handle loading states during auth operations

## Dependencies

- [x] Task 8 (Foundation Setup) complete
- [x] `AuthBloc` exists
- [x] Auth use cases exist
- [x] Firebase Auth configured

## Effort Estimate

- Size: S
- Hours: 3-4
- Parallel: true (can run alongside tasks 10, 11, 12)

## Definition of Done

- [x] All auth routes work
- [x] All 3 auth providers functional
- [x] Auth state navigation correct
- [x] Manual testing with real accounts
